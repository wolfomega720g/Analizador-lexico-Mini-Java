package Jflextest;

public enum Token {
	igual, suma, variable, numero, multiplicacion, resta , decimal ,division, incremento,exponente,IF, THEN, ELSE, ID, FOR, WHILE, DO, BREAK,LLAVES,CORCHETES,PUNTOCOMA,OR,AND,EQUALS,GREATER,LOWER,GREATER_EQUALS,LOWER_EQUALS,ERROR
}
